# 🚀 快速开始指南

## 1️⃣ 构建项目

```bash
# 安装依赖
make install

# 构建应用
make build
```

构建成功后，会在 `bin/` 目录生成可执行文件 `agent-team-monitor`（约 5MB）。

## 2️⃣ 测试运行

### 选项 A: 使用测试数据（推荐新手）

```bash
# 创建测试团队和任务数据
./test-setup.sh

# 运行监控器
./bin/agent-team-monitor
```

你会看到一个测试团队，包含 3 个 agents 和 3 个任务。

### 选项 B: 监控真实的 Claude teams

如果你已经在使用 Claude Code 并创建了 agent teams：

```bash
# 直接运行
./bin/agent-team-monitor
```

监控器会自动检测 `~/.claude/teams/` 中的所有团队。

## 3️⃣ 界面操作

运行后你会看到：

```
🤖 Claude Agent Team Monitor
Last updated: 23:05:42

📊 Claude Processes (1 running)
  PID: 12345 | Uptime: 2h 15m

👥 Active Teams (1 teams)
╭─────────────────────────────────────╮
│ Team: test-team                     │
│ Created: 2026-02-10 20:30           │
│                                     │
│ Agents:                             │
│   • researcher [Explore] - IDLE     │
│   • developer [general] - WORKING   │
│     (Task: task-2)                  │
│   • tester [test-runner] - IDLE     │
│                                     │
│ Tasks:                              │
│   [task-1] Research - COMPLETED     │
│   [task-2] Implement - IN PROGRESS  │
│   [task-3] Test - PENDING           │
╰─────────────────────────────────────╯

Press 'r' to refresh | 'q' to quit
```

### 快捷键
- `r` - 手动刷新数据
- `q` 或 `Ctrl+C` - 退出程序

## 4️⃣ 状态说明

### Agent 状态
- 🟢 **WORKING** - Agent 正在执行任务（有 in_progress 状态的任务）
- 🟡 **IDLE** - Agent 空闲（没有进行中的任务）
- ⚪ **COMPLETED** - Agent 已完成任务

### Task 状态
- 🟢 **IN PROGRESS** - 任务进行中
- 🟡 **PENDING** - 任务待处理
- ⚪ **COMPLETED** - 任务已完成

## 5️⃣ 清理测试数据

```bash
# 删除测试团队和任务
rm -rf ~/.claude/teams/test-team ~/.claude/tasks/test-team
```

## 6️⃣ 全局安装（可选）

```bash
# 安装到系统路径
make install-global

# 之后可以在任何地方运行
agent-team-monitor
```

## 7️⃣ 多平台构建

```bash
# 构建所有平台版本
make build-all

# 生成的文件：
# bin/agent-team-monitor-darwin-amd64  (macOS Intel)
# bin/agent-team-monitor-darwin-arm64  (macOS Apple Silicon)
# bin/agent-team-monitor-linux-amd64   (Linux x86_64)
# bin/agent-team-monitor-linux-arm64   (Linux ARM64)
```

## 🎯 使用场景

### 场景 1: 开发调试
在开发 Claude Code agent teams 时，实时监控 agents 的状态和任务进度。

### 场景 2: 任务追踪
查看哪些任务正在进行，哪些已完成，哪些还在等待。

### 场景 3: 性能分析
监控 Claude Code 进程的运行时间和资源使用。

### 场景 4: 团队协作
多人协作时，了解各个 agent 的工作状态。

## 🐛 常见问题

### Q: 显示 "No active teams found"
**A:** 确保 `~/.claude/teams/` 目录存在且包含团队配置文件。可以运行 `./test-setup.sh` 创建测试数据。

### Q: 显示 "No Claude processes detected"
**A:** 监控器查找包含 "claude" 或 "claude-code" 的进程。确保 Claude Code 正在运行。

### Q: 权限错误
**A:** 确保你有读取 `~/.claude/` 目录的权限。

### Q: 数据不更新
**A:** 按 `r` 手动刷新，或等待自动刷新（每秒一次）。

## 📚 更多信息

- 详细文档: [README.md](README.md)
- 中文文档: [README.zh-CN.md](README.zh-CN.md)
- 项目总结: [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md)

## 💡 提示

1. 监控器会自动刷新，无需手动操作
2. 文件系统变化会立即反映（100ms 延迟）
3. 进程信息每 5 秒更新一次
4. 所有操作都是只读的，不会修改任何文件
5. 可以同时监控多个团队

## 🎉 开始使用

```bash
# 一键启动（包含测试数据）
./test-setup.sh && ./bin/agent-team-monitor
```

祝你使用愉快！🚀
